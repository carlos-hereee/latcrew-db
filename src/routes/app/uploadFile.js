module.exports = (req, res) => {
  const body = req.body;
  // console.log("req.headers", req.headers);
  console.log("body", body);
  // store paths and details on db
  // store img in public folder
  // console.log("upload file ", req.user);
};
